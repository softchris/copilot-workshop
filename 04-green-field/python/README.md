1. Create env

    ```bash
    python -m venv env
    ```

1. Activate the environment

    ```bash
    .\env\Scripts\activate
    ```

    or 

    ```bash
    source env/bin/activate
    ```

1. Install dependencies

    ```bash
    pip install pygame
    ```